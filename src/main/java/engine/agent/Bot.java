package engine.agent;

import engine.map.Map;
import engine.object.Object;

import java.util.List;

public class Bot extends Agent {
    @Override
    public Action getAction(Map map, List<Agent> agents, List<Object> objects) {
        return null;
    }
}
