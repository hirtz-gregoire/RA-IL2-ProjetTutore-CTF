package engine.agent;

import engine.map.GameMap;
import engine.object.Object;

import java.util.List;

public class Bot extends Agent {
    @Override
    public Action getAction(GameMap map, List<Agent> agents, List<Object> objects) {
        return null;
    }
}
